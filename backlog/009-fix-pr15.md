---
rank: 0
title: Address Claude review on PR #15
area: review-fix
---
# Address Claude review on PR #15
PR: #15
Branch: auto/atr-dynamic-stops
Resolves-Backlog: 008-ATR-dynamic-stops-and-targets 009-fix-pr15

## Why
Claude Opus 4.8 requested changes on PR #15 (round 3).

## Required fixes
- In scripts/backtest.py, decouple the stop computation from the exit decision: compute `stop_price` (including the ATR override) as an unconditional block, then evaluate the exit as its own `if in_position and (composite <= -ENTRY_THRESHOLD or price < stop_price):` rather than chaining it as an `elif` off the `if stop_loss_method == "atr"` statement.
- Preserve the original if/elif relationship between the entry branch and the exit branch (they must remain a mutually-exclusive chain), while making the ATR stop_price selection a plain nested computation that runs regardless of method.
- Add/adjust a backtest test exercising stop_loss_method="atr" that asserts an open position actually exits (stop-hit and composite-reversal), which would have caught this.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/atr-dynamic-stops` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
