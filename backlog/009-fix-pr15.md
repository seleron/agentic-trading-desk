---
rank: 0
title: Address Claude review on PR #15
area: review-fix
---
# Address Claude review on PR #15
PR: #15
Branch: auto/atr-dynamic-stops
Resolves-Backlog: 008-ATR-dynamic-stops-and-targets 009-fix-pr15

## Why
Claude Opus 4.8 requested changes on PR #15 (round 2).

## Required fixes
- In scripts/backtest.py, move the stop_price computation (the `stop_price = ...` line and the `if stop_loss_method == "atr" and in_position:` block) ABOVE the entry `if`/exit `elif` chain, and keep the exit condition as an `elif` of the original entry `if` (e.g. `elif in_position and (composite <= -ENTRY_THRESHOLD or price < stop_price):`). As written, the exit elif binds to the ATR `if`, so ATR-mode positions never exit.
- Add or extend a backtest test that runs stop_loss_method='atr' end-to-end and asserts at least one stop-driven exit occurs, to catch this class of regression (the current gate passes despite the broken exit path).

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/atr-dynamic-stops` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
