---
rank: 0
title: Address Claude review on PR #14
area: review-fix
---
# Address Claude review on PR #14
PR: #14
Branch: auto/watchlist-scripts
Resolves-Backlog: (none — new feature no originating item) 007-fix-pr14

## Why
Claude Opus 4.8 requested changes on PR #14 (round 2).

## Required fixes
- watchlist_scan.py: drop the borsapy dependency and fetch BIST bars via yfinance with the .IS suffix (matching the repo's established provider), since borsapy was removed as non-working.
- unified_data_provider.py: route BIST through yfinance '.IS' instead of borsapy Tickers to match the current data layer, or remove the borsapy branch.
- us_watchlist_scan.py: remove the dead `if isinstance(df.columns, pd.MultiIndex if False else ...)` line — `pd` is never imported and the check is a no-op.
- watchlist_scan.py: fix the `rsi` field expression `round(sc.get('raw_components',{}).get('momentum') is not None and (q['rsi'] or 0), 1)` which rounds a boolean/short-circuit result; use rsi_val consistently.
- Confirm the `sys.path.insert(..., 'scripts')` is correct — when run from scripts/ it appends a nonexistent scripts/scripts; imports work only via the auto-added script dir, so the insert is misleading/dead.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/watchlist-scripts` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
