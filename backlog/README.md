# Agentic Trading Desk — Backlog

Prioritized improvement items for autonomous development loop. Items are ranked 1-9, with lower numbers = higher priority. Fix items (review-fix type) get Rank 0 and auto-select when their PR branch is open.

See `scripts/hermes/loop-context.sh` for selection logic.
