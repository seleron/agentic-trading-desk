---
rank: 0
title: Address Claude review on PR #10
area: review-fix
---
# Address Claude review on PR #10
PR: #10
Branch: auto/fix-validation-tracker-bugs
Resolves-Backlog: 008-fix-pr10

## Why
Claude Opus 4.8 requested changes on PR #10 (round 5).

## Required fixes
- Make the restored ci.sh gate pass locally before resubmitting — run `python3 scripts/test_validation_tracker.py` and the full suite, fix the failing/uncollectable tests, and do not raise metrics/baseline.json minTests to mask a failure; only bump minTests once the new tests genuinely pass under the trusted gate.
- Split scope: keep this PR to the validation_tracker fixes only. The orchestrator.py yfinance→borsapy migration is a separate concern (and largely duplicates prior commit 145df28) — move it to its own PR.
- Verify the borsapy fallback path actually works at runtime: `borsapy.Tickers(sym).history(period=..., interval='1d')` must return a pandas DataFrame with Open/High/Low/Close/Volume columns and a datetime index; confirm the package/API exists and is a declared dependency, otherwise the fallback and _get_morning_closes silently yield no data.
- Fix _get_morning_closes/_get_eod_closes semantics: both return the same last daily candle, so 'morning' and 'EOD' close are identical and delta_pct is always ~0 — the tracker cannot measure intraday prediction accuracy as designed; source a real intraday/open-vs-close price or document the daily-over-daily semantics explicitly.
- Remove dead/misleading mock scaffolding (_make_mock_yfinance is unused; comments/flags still say 'yfinance') and the unreachable simulated-score branch in main() morning mode.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/fix-validation-tracker-bugs` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
