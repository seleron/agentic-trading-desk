---
rank: 0
title: Address Claude review on PR #10
area: review-fix
---
# Address Claude review on PR #10
PR: #10
Branch: auto/fix-validation-tracker-bugs
Resolves-Backlog: 008-fix-pr10

## Why
Claude Opus 4.8 requested changes on PR #10 (round 2).

## Required fixes
- Add `import pandas as pd` to scripts/validation_tracker.py (it uses pd.Timestamp in _get_morning_closes but never imports pandas — NameError at runtime).
- Fix the failing gate: run the restored ci.sh/tests and make them pass by fixing code, not by adjusting baseline.json. Only bump minTests to 193 after the suite genuinely passes.
- Update scripts/test_validation_tracker.py: _make_mock_yfinance and the borsapy-backed paths (prepare_morning_snapshot, _get_morning_closes) must mock borsapy.Tickers instead of yfinance so tests don't perform live network fetches; the current mocks target the wrong dependency.
- Reconcile the borsapy API assumptions: orchestrator/validation_tracker call `Tickers(sym).history(...)` and index by 'Close'/'Open'/'High'/'Low'/'Volume' — verify borsapy actually exposes this shape and that morning symbols use '.IS' suffix consistently (validation_tracker adds .IS, orchestrator does not).
- Remove/replace the simulated pseudo-score path in main() (`50.0 + (close_price % 10)*2`) — it silently records fake scores instead of real scoring_engine output, so the tracker validates nothing meaningful in the CLI 'morning' mode.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/fix-validation-tracker-bugs` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
