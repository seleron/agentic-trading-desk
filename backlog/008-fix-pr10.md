---
rank: 0
title: Address Claude review on PR #10
area: review-fix
---
# Address Claude review on PR #10
PR: #10
Branch: auto/fix-validation-tracker-bugs
Resolves-Backlog: 008-fix-pr10

## Why
Claude Opus 4.8 requested changes on PR #10 (round 10).

## Required fixes
- Make the independent gate pass with the restored ci.sh/tests before any baseline change — do not merge with a red gate.
- Do not raise metrics/baseline.json minTests (160→194) until the gate is green with the new tests actually running and counted; the bump currently rides along with a failing gate.
- Verify `borsapy>=0.10.0` actually installs and imports in CI; if it is not a real/available package, the morning/eod fetch path (_get_morning_closes) delivers no value at runtime and the feature only looks complete.
- Fix the EOD 'flat price' semantics: for score>=60, `eod_close > morning_close` marks an exactly-flat move as INCORRECT while the docstring says 'price went up' — confirm this is intended and that the test asserting flat→INCORRECT matches the decision band contract.
- Reconcile prepare_morning_snapshot's two-pass record (records returned from the first pass lack close_price, so the returned list is stale relative to what is persisted on the re-record).

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/fix-validation-tracker-bugs` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
