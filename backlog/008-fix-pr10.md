---
rank: 0
title: Address Claude review on PR #10
area: review-fix
---
# Address Claude review on PR #10
PR: #10
Branch: auto/fix-validation-tracker-bugs
Resolves-Backlog: 008-fix-pr10

## Why
Claude Opus 4.8 requested changes on PR #10 (round 4).

## Required fixes
- Make the restored/independent gate pass on the actual code — do not rely on editing baseline.json to reach green. Investigate and fix the real test/import failures the gate reports.
- Justify or revert the metrics/baseline.json minTests 160→193 bump; only raise it if the new tests genuinely and deterministically pass in the trusted gate, and never as a means to force the gate green.
- Add borsapy to requirements (it is now imported by orchestrator.py fallbacks and validation_tracker._get_morning_closes); confirm it exposes Tickers(...).history(period=,interval=) as used, otherwise the fetch paths raise at runtime.
- Fix prepare_morning_snapshot/CLI morning mode: currently when a real close_price is fetched the symbol is 'skipped' with a WARN, so morning snapshots are only recorded when NO price data exists — the intended path (score + real price) never records. Wire real scoring_engine output through instead of skipping.
- Verify the weekly-timeframe path in orchestrator now fetches DAILY data via borsapy but still treats it as 'closes_weekly' (multi-timeframe verification is silently reduced to single-timeframe); either resample to weekly or drop the misleading weekly claim.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/fix-validation-tracker-bugs` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
