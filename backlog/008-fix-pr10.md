---
rank: 0
title: Address Claude review on PR #10
area: review-fix
---
# Address Claude review on PR #10
PR: #10
Branch: auto/fix-validation-tracker-bugs
Resolves-Backlog: 008-fix-pr10

## Why
Claude Opus 4.8 requested changes on PR #10 (round 14).

## Required fixes
- Drop the new `borsapy` dependency and implement `_get_morning_closes`/`_get_eod_closes` using the already-present `yfinance` with `.IS` suffixes (the established BIST path), or justify borsapy with a verified, installable package and pin it correctly.
- Fix the inverted morning-mode logic in main(): currently when borsapy returns a real close_price the code prints a WARN and `continue`s (skipping the symbol), so records are only created when close_price is None and --score is passed. The automated fetch path must actually produce scored quotes.
- Actually integrate scoring_engine output (or clearly document that --score is mandatory) so morning snapshots carry real scores rather than requiring manual CLI scores.
- Remove the redundant double price fetch: main() calls `_get_morning_closes` and then `prepare_morning_snapshot` (without close_prices) fetches again.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/fix-validation-tracker-bugs` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
