---
rank: 0
title: Address Claude review on PR #10
area: review-fix
---
# Address Claude review on PR #10
PR: #10
Branch: auto/fix-validation-tracker-bugs
Resolves-Backlog: 008-fix-pr10

## Why
Claude Opus 4.8 requested changes on PR #10 (round 8).

## Required fixes
- Make the gate pass: run the restored ci.sh/tests locally and fix whatever fails before merge — do not adjust baseline.json or tests to force a green gate.
- Split into one concern per PR: keep the validation_tracker.py + test_validation_tracker.py addition here, and move the yfinance→borsapy migration in orchestrator.py and requirements.txt into a separate PR.
- Verify the borsapy dependency is real and its API matches usage: confirm `from borsapy import Tickers`, that `.history(period=..., interval='1d')` returns a DataFrame with Open/High/Low/Close/Volume columns and a datetime index, and that `.IS` symbol suffixing is correct — otherwise every BIST fetch path silently falls through to the except branch.
- Fix stale/misleading references left by the migration: `_make_mock_yfinance`, 'via yfinance' comments/docstrings, and the '--mode morning' comment 'Fetch morning prices via yfinance' all still say yfinance.
- Reconcile the weekly resample path: orchestrator now calls `hist_daily.resample('W-FRI')` on the borsapy DataFrame, which requires a proper DatetimeIndex — confirm borsapy returns one, or the resample raises and silently drops to the daily proxy.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/fix-validation-tracker-bugs` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
