---
rank: 0
title: Address Claude review on PR #10
area: review-fix
---
# Address Claude review on PR #10
PR: #10
Branch: auto/fix-validation-tracker-bugs
Resolves-Backlog: 008-fix-pr10

## Why
Claude Opus 4.8 requested changes on PR #10 (round 3).

## Required fixes
- Make the independent gate pass without touching baseline.json/tests: run the restored ci.sh locally and fix the actual failures rather than lowering the bar or padding test count.
- Do not bump metrics/baseline.json minTests (160→193) in the same PR that adds the tests being counted; revert the baseline change unless a backlog item explicitly requires it, and justify any minTests change separately.
- Fix the borsapy mock in test_validation_tracker.py: real pandas DataFrame.iloc is not callable — mock_hist.iloc[-1] must return a Series, not MagicMock(return_value=...). Build a real pandas DataFrame fixture so tests validate the actual _get_morning_closes/.IS code path.
- Confirm borsapy.Tickers(sym).history(...) returns a DataFrame with Open/High/Low/Close/Volume columns and a datetime index as assumed in both orchestrator.py and validation_tracker.py; add a guard/test proving the column names match the real library.
- Remove the double record_morning_score call in prepare_morning_snapshot (it inserts once with null close, then re-fetches live prices and inserts again) or gate the network fetch behind an injectable provider so tests are deterministic and offline.
- Reduce scope: this PR mixes the validation_tracker feature+tests, an orchestrator yfinance→borsapy migration, and a baseline bump — split into single-concern PRs.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/fix-validation-tracker-bugs` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
