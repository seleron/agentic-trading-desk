---
rank: 0
title: Address Claude review on PR #10
area: review-fix
---
# Address Claude review on PR #10
PR: #10
Branch: auto/fix-validation-tracker-bugs
Resolves-Backlog: 008-fix-pr10

## Why
Claude Opus 4.8 requested changes on PR #10 (round 7).

## Required fixes
- Make the restored/independent gate pass without editing the baseline to match: run the real tests and fix the code, not the threshold. Only raise minTests in baseline.json once the suite genuinely passes at that count.
- Verify borsapy>=0.10.0 actually installs from PyPI and exposes `Tickers(...).history(period=..., interval=...)` returning a DataFrame with Open/High/Low/Close/Volume and a datetime index. If it does not resolve, the borsapy migration is an unmet premise — keep yfinance or pin a real package.
- Confirm the borsapy API shape assumed in _get_morning_closes (iloc[-1], .index, capitalized columns) and orchestrator resampling matches the real library; add an import/availability guard so a missing borsapy degrades gracefully instead of crashing the pipeline.
- Reconcile scope: this branch is 'fix-validation-tracker-bugs' but also swaps the BIST data source across orchestrator.py and requirements — split the data-source migration from the validation-tracker work (one concern per PR).
- Fix the CLI comment/inconsistency (main() morning mode comment says 'via yfinance' but calls borsapy) and ensure test mock helper _make_mock_yfinance name/behavior matches the borsapy contract.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/fix-validation-tracker-bugs` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
