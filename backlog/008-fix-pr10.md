---
rank: 0
title: Address Claude review on PR #10
area: review-fix
---
# Address Claude review on PR #10
PR: #10
Branch: auto/fix-validation-tracker-bugs
Resolves-Backlog: 008-fix-pr10

## Why
Claude Opus 4.8 requested changes on PR #10 (round 6).

## Required fixes
- Make the trusted gate pass without touching the gate: run the restored ci.sh + tests and fix the underlying failures rather than adjusting metrics/baseline.json; only bump minTests once the full suite genuinely passes at that count.
- Fix the NameError in `main()` eod mode: when `eod_prices` is truthy the code falls through to `print(json.dumps(records...))` without ever calling `record_eod_actuals`, so `records` is undefined. Add the `records = record_eod_actuals(args.date, eod_prices, args.db)` branch for the success path.
- Verify `borsapy` is importable in the CI/test environment — the orchestrator and validation_tracker now hard-depend on `from borsapy import Tickers`; if it is not installed the new module import and integration paths fail. Add it to requirements if the runtime truly provides it, otherwise the feature only looks done.
- Fix `_make_mock_yfinance` and any tests relying on it: `mock_hist.iloc = MagicMock(return_value=...)` models iloc as callable, but the code uses `hist_bp.iloc[-1]` (subscript). The mock will not behave like the real path.
- Confirm the weekly-timeframe fetch still delivers value: borsapy is called with `interval='1d'` but the result is treated as `closes_weekly` with only a comment claiming a resample-to-weekly that never happens, so weekly == daily and the multi-timeframe check is effectively a no-op.

## Acceptance
- Trusted gate passes: `bash scripts/ci.sh` prints GATE PASSED
- Every fix above is addressed in the diff; no regressions
- Re-review approves

## Constraints
- UPDATE the existing branch `auto/fix-validation-tracker-bugs` (do NOT open a new PR)
- Do NOT edit scripts/ci.sh, metrics/baseline.json, or test files to force a pass
