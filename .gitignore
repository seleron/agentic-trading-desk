__pycache__/
*.db
data/
outputs/
# Host-specific Hermes loop config (delivery targets, chat ids) — never commit.
scripts/hermes/loop.env
us_watchlist_results.json

# Generated watchlist scan output (script artifact, not source).
